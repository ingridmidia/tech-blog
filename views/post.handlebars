<div class="row">
    <div class="card col-11 mx-auto m-4 my-card">
        <div class="card-header bg-lilac">
            <h5>{{post.title}}</h5>
            <h9>Posted by {{post.user.username}} on {{post.createdAt}}</h9>
        </div>
        <div class="card-body">
            <p class="card-text">{{post.content}}</p>
        </div>
    </div>
</div>

<div class="row">
    <div class="card col-11 mx-auto m-4 my-card">
        <div class="card-header bg-lilac">
            <h5>Comments</h5>
        </div>
        <div class="card-body">
            <form>
                <div class="mb-3">
                    <input type="text" class="form-control" id="comment" placeholder="Add a comment">
                </div>
                <button type="submit" class="btn btn-outline-dark btn-sm">Comment</button>
            </form>
            {{#each comments as |comment|}}
            <p class="card-text">{{comment.content}}</p>
            <p class="card-text">-{{comment.user.username}}, {{comment.createdAt}}</p>
            <hr>
            {{/each}}
        </div>
    </div>
</div>